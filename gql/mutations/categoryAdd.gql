mutation CategoryAdd(
    $addData: CategoryAddInput!
)
{
    categoryAdd(categoryAddData: $addData) {
        categories { 
            name
        }

        error { 
            name
            field
        }
    }
}
